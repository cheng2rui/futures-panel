/* GridStack 10.3.1 - Browser UMD wrapper */
(function() {
  if (typeof window !== 'undefined') {
    // Expose GridStack to window via a temporary script load
    window.__gridstack_loaded = false;
  }
})();
